# App environment
NODE_ENV=development
# 站点对外完整地址（用于 sitemap、OG、回调等）
NEXT_PUBLIC_SITE_URL=https://your-domain.example.com

# i18n
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_SUPPORTED_LOCALES=en,zh

# Database (Supabase Postgres / PgBouncer)
# drizzle/config 与脚本优先使用 DIRECT_URL，其次 DATABASE_URL
# DIRECT_URL: 直连 5432，DATABASE_URL: 连接池 6543（pgbouncer=true）
DIRECT_URL=postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@host:6543/dbname?pgbouncer=true&sslmode=require

# Clerk（如项目使用 Clerk 中间件）
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxx
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# Next.js optional
# 用于加密 cookies（如有自定义），否则可删除
# NEXTAUTH_SECRET=change_me_or_remove

# 其他第三方服务（如需要再补充）
# OPENAI_API_KEY=
# SENTRY_DSN=
# RESEND_API_KEY=